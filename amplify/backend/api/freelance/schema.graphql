type Job @model @searchable {
  description: String!
  numberOfDays: Int
  hourlyRate: Float
  hoursPerWeek: Float
  id: ID!
  location: String!
  pricingStructure: String
  remote: Boolean
  title: String!
  type: String!
}
